import MediaLibrary from '@/studio/media/components/MediaLibrary';

export const metadata = {
  title: 'Media Library | Amalgamic Studio',
};

export default function StudioMediaPage() {
  return <MediaLibrary />;
}
